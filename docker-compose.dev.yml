services:
  api:
    command:
      - .venv/bin/uvicorn
      - atlas.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --reload
      - --reload-dir
      - /app/src
    volumes:
      - ./src:/app/src